# Tarefa 5.0: Frontend: Proteção de Rotas

<critical>Ler os arquivos de prd.md e techspec.md desta pasta, se você não ler esses arquivos sua tarefa será invalidada</critical>

## Visão Geral

<complexity>MEDIUM</complexity>

Implementar route guards no React Router para proteger rotas administrativas, exigindo token no sessionStorage, redirecionando não autenticados para /auth/login e autenticados nas telas de login para /dashboard, além de ajustar o logout para limpar o sessionStorage.

<requirements>
- Implementar route guards em src/routes/router.jsx
- Proteger rotas administrativas exigindo token
- Redirecionar não autenticados para /auth/login
- Redirecionar autenticados nas telas de login para /dashboard
- Ajustar logout para limpar sessionStorage
</requirements>

## Subtarefas

- [ ] 5.1 Analisar estrutura atual de rotas em src/routes/router.jsx
- [ ] 5.2 Criar componente RequireAuth ou similar para proteção de rotas
- [ ] 5.3 Implementar verificação de token em sessionStorage
- [ ] 5.4 Proteger rotas administrativas com RequireAuth
- [ ] 5.5 Implementar redirecionamento para /auth/login quando não autenticado
- [ ] 5.6 Implementar redirecionamento para /dashboard quando autenticado em telas de login
- [ ] 5.7 Ajustar página de logout para limpar sessionStorage usando authSession.clear()
- [ ] 5.8 Testar proteção de rotas manualmente
- [ ] 5.9 Testar redirecionamento de autenticado em telas de login manualmente
- [ ] 5.10 Testar logout manualmente

## Detalhes de Implementação

Referenciar a seção "Design de Implementação", "Pontos de Integração" e "Arquivos relevantes" do techspec.md para contratos e integrações.

Requisitos funcionais (seção 5 do PRD):
- Rotas administrativas devem exigir token no sessionStorage
- Usuário sem token deve ser redirecionado para /auth/login
- Usuário autenticado que acessar telas de login deve ser redirecionado para /dashboard
- O logout deve remover o token do sessionStorage

authSession (techspec.md):
```js
export const authSession = {
  getToken: () => sessionStorage.getItem('milatec:accessToken'),
  setToken: token => sessionStorage.setItem('milatec:accessToken', token),
  clear: () => sessionStorage.removeItem('milatec:accessToken'),
};
```

React Router (techspec.md):
- Usa Navigate, useLocation e useNavigate para proteger rotas e preservar destino quando aplicável

Arquivos relevantes (techspec.md):
- `JS/src/routes/index.jsx`
- `JS/src/routes/router.jsx`
- `JS/src/app/(other)/auth/logout/page.jsx`

Implementação sugerida para RequireAuth:
```jsx
function RequireAuth({ children }) {
  const token = authSession.getToken();
  const location = useLocation();

  if (!token) {
    return <Navigate to="/auth/login" state={{ from: location }} replace />;
  }

  return children;
}
```

Implementação sugerida para redirecionamento de autenticado em telas de login:
```jsx
// Em /auth/login e /auth/login-pin
if (authSession.getToken()) {
  return <Navigate to="/dashboard" replace />;
}
```

## Critérios de Sucesso

- Route guards implementados em src/routes/router.jsx
- Rotas administrativas exigem token no sessionStorage
- Usuário sem token é redirecionado para /auth/login
- Usuário autenticado em telas de login é redirecionado para /dashboard
- Logout limpa sessionStorage corretamente
- Navegação preserva destino original quando aplicável

## Testes da Tarefa

- [ ] Testes unitários para RequireAuth (com token, sem token)
- [ ] Testes unitários para redirecionamento de autenticado em telas de login
- [ ] Teste manual de acesso a rota protegida sem token (deve redirecionar para login)
- [ ] Teste manual de acesso a rota protegida com token (deve permitir acesso)
- [ ] Teste manual de acesso a /auth/login com token (deve redirecionar para /dashboard)
- [ ] Teste manual de acesso a /auth/login-pin com token (deve redirecionar para /dashboard)
- [ ] Teste manual de logout (deve limpar sessionStorage e redirecionar)
- [ ] Testes E2E com Playwright (seção "Abordagem de Testes" do techspec.md)

<critical>SEMPRE CRIE E EXECUTE OS TESTES DA TAREFA ANTES DE CONSIDERÁ-LA FINALIZADA</critical>

## Arquivos relevantes
- `JS/src/routes/router.jsx`
- `JS/src/routes/index.jsx`
- `JS/src/app/(other)/auth/logout/page.jsx`
- `JS/src/services/authSession.js`
- `tasks/prd-login-pin/prd.md`
- `tasks/prd-login-pin/techspec.md`

## Atribuição
**Assignee:** Julia
**Status:** Concluído
