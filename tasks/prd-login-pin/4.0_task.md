# Tarefa 4.0: Frontend: Serviços de Autenticação

<critical>Ler os arquivos de prd.md e techspec.md desta pasta, se você não ler esses arquivos sua tarefa será invalidada</critical>

## Visão Geral

<complexity>MEDIUM</complexity>

Criar os serviços de autenticação do frontend (authApi e authSession) para centralizar chamadas HTTP e gerenciar o token JWT no sessionStorage, além de configurar a variável de ambiente VITE_API_BASE_URL.

<requirements>
- Criar authApi.js com funções requestPin e verifyPin
- Criar authSession.js para gerenciar sessionStorage
- Configurar VITE_API_BASE_URL em .env
- Implementar tratamento padronizado de erros
- Documentar configuração em README
</requirements>

## Subtarefas

- [ ] 4.1 Criar arquivo JS/src/services/authApi.js
- [ ] 4.2 Implementar função authFetch com tratamento de erros
- [ ] 4.3 Implementar função requestPin
- [ ] 4.4 Implementar função verifyPin
- [ ] 4.5 Criar arquivo JS/src/services/authSession.js
- [ ] 4.6 Implementar getToken, setToken e clear em authSession
- [ ] 4.7 Configurar VITE_API_BASE_URL no .env do frontend
- [ ] 4.8 Documentar configuração em README.md
- [ ] 4.9 Adicionar VITE_API_BASE_URL em JS/src/context/constants.js (se necessário)

## Detalhes de Implementação

Referenciar a seção "Interfaces Principais", "Pontos de Integração" e "Arquivos relevantes" do techspec.md para contratos e configurações.

authApi (techspec.md):
```js
export async function requestPin(email) {
  return authFetch('/auth/request-pin', { email });
}

export async function verifyPin(email, pin) {
  return authFetch('/auth/verify-pin', { email, pin });
}
```

authSession (techspec.md):
```js
export const authSession = {
  getToken: () => sessionStorage.getItem('milatec:accessToken'),
  setToken: token => sessionStorage.setItem('milatec:accessToken', token),
  clear: () => sessionStorage.removeItem('milatec:accessToken'),
};
```

authFetch deve:
- Usar VITE_API_BASE_URL para montar URL completa
- Usar fetch nativo
- Enviar Authorization header quando token disponível
- Tratar erros HTTP (401, 403, 429, 500)
- Parsear resposta JSON
- Propagar mensagens de erro do backend

VITE_API_BASE_URL (techspec.md):
- Define a URL do backend
- Exemplo local: http://localhost:3000
- Deve ser documentado em README ou documento operacional do frontend

## Critérios de Sucesso

- authApi.js criado com funções requestPin e verifyPin
- authFetch implementa tratamento padronizado de erros
- authSession.js criado com getToken, setToken e clear
- VITE_API_BASE_URL configurado no .env
- Configuração documentada em README.md
- authApi usa VITE_API_BASE_URL corretamente
- authFetch propaga mensagens de erro do backend
- authSession usa chaves corretas do sessionStorage

## Testes da Tarefa

- [ ] Testes unitários para authApi (montagem de URL, parse de sucesso, propagação de erros)
- [ ] Testes unitários para authSession (leitura, escrita e limpeza do token)
- [ ] Teste manual de chamada a requestPin com e-mail válido
- [ ] Teste manual de chamada a verifyPin com PIN válido
- [ ] Teste manual de tratamento de erros (401, 429, 500)
- [ ] Validação de VITE_API_BASE_URL no .env

<critical>SEMPRE CRIE E EXECUTE OS TESTES DA TAREFA ANTES DE CONSIDERÁ-LA FINALIZADA</critical>

## Arquivos relevantes
- `JS/src/services/authApi.js` (novo)
- `JS/src/services/authSession.js` (novo)
- `JS/.env`
- `JS/README.md`
- `JS/src/context/constants.js`
- `tasks/prd-login-pin/prd.md`
- `tasks/prd-login-pin/techspec.md`

## Atribuição
**Assignee:** Julia
**Status:** Concluído
